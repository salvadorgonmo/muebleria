'use strict'

// we need to know the host that this API will have.
const config = {
  SERVER_HOST: '',
  PORT: 3000,
  secretOrKey: '$3cr3t-furn17ur3570r3',
  saltRounds: 10
}

module.exports = config
